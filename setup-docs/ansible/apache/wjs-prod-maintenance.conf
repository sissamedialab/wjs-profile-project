<VirtualHost *:80>
    ServerName journals.sissamedialab.it
    ServerAlias  jcom.sissa.it
    ServerAlias  jcomal.sissa.it

    DocumentRoot /var/www/hosts/journals/html
    CustomLog ${APACHE_LOG_DIR}/janeway80-access.log combined
    ErrorLog  ${APACHE_LOG_DIR}/janeway80-error.log

    RewriteEngine On
    RewriteRule ^(.*)$          https://%{SERVER_NAME}$1 [last,redirect=301]
</VirtualHost>


<VirtualHost *:443>
    Define NAME journals
    ServerName ${NAME}.sissamedialab.it
    Define LOG_NAME ${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>


<VirtualHost *:443>
    Define NAME jcom
    ServerName ${NAME}.sissa.it
    Define LOG_NAME ${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>


<VirtualHost *:443>
    Define NAME jcomal
    ServerName ${NAME}.sissa.it
    Define LOG_NAME ${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>



<VirtualHost *:443>
    Define NAME jquant
    ServerName ${NAME}.sissa.it
    Define LOG_NAME	${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>



<VirtualHost *:443>
    Define NAME jhep
    ServerName ${NAME}.sissa.it
    Define LOG_NAME	${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>



<VirtualHost *:443>
    Define NAME jcap
    ServerName ${NAME}.sissa.it
    Define LOG_NAME	${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>



<VirtualHost *:443>
    Define NAME jstat
    ServerName ${NAME}.sissa.it
    Define LOG_NAME	${NAME}

    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>



<VirtualHost *:443>
    Define NAME jinst
    ServerName ${NAME}.sissa.it
    Define LOG_NAME	${NAME}
    DocumentRoot /var/www/maint-pages
    RewriteEngine On
    RewriteCond %{SCRIPT_FILENAME} !${NAME}.maintenance.html
    RewriteRule ^(.*)$ /$1 [R=503,L]
    ErrorDocument 503 /${NAME}.maintenance.html
    Header Set Pragma "no-cache"
    Header Set Expires "Thu, 1 Jan 1970 00:00:00 GMT"
    Header Set Cache-Control "max-age=0, no-store, no-cache, must-revalidate"
    Header Unset ETag
    FileETag None

    Include "conf-available/wjs/apache-log-and-ssl.inc"
</VirtualHost>


# vim: syntax=apache ts=4 sw=4 sts=4 sr et
