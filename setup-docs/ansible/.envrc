# Default to the test inventory. Production is opt-in only, and must be
# selected explicitly with `-i inventories/prod.yml` (see inventories/prod.yml).
export ANSIBLE_INVENTORY=inventories/test.yml
export ANSIBLE_NOCOWS=1
export ANSIBLE_COW_SELECTION=random  # check cowsay -l 😉
